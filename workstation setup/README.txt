Setting Up Workstation

copy the package control file into the installed packages in sublime appdata
then restart sublime, search for and install the following packages

if you have admin rights you can install the javac linter, just make sure the javac directory is in the path variable

packages to get:
	alignment and brackethighlighter